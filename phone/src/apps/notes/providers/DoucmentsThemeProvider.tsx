import { createAppThemeProvider } from '../../../os/apps/utils/createAppThemeProvider';
import notesTheme from '../documents.theme';

export const DocumentsThemeProvider = createAppThemeProvider(notesTheme);
